# Force selecting a specific version, not necessarily the newest.  This way all
# versions that exist can be enumerated as package files, including those newer
# than the current live version.
#
# These should be set to exactly 1 higher than the version you want included,
# even if the resulting +1 version does not actually exist.
>=sys-firmware/chromeos-tcon-firmware-atlas-fhd-1.5
>=sys-firmware/chromeos-tcon-firmware-atlas-uhd-1.5
