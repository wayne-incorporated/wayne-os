The minimal profile disables most USE flags to keep the base image small.
Resulting image supports basic networking, and can receive updates. Though it
gives you a usable shell, if you base your device on this profile, you may want
to add more packages (such as bash, vim, openssh) to have a fuller environment.
