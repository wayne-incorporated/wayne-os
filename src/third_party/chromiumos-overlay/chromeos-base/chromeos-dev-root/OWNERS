# This isn't a great match, but since dev_install is heavily geared towards
# creating a dev-image on the fly in a release image, it's about the closest
# we have currently.
include /chromeos-base/dev-install/OWNERS
