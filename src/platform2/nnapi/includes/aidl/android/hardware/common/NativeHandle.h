// This code is autogenerated from the AIDL definitions in
// AOSP /hardware/interfaces/neuralnetworks/aidl.

// This code HAS been modified:
// * readFromParcel is an empty implementation and returns 0
// * writeToParcel is an empty implementation and returns 0

// To regenerate, checkout AOSP and, navigate to
// hardware/interfaces/neuralnetworks/aidl, run:
// $ lunch
// $ mm
// $ cd
// ./out/soong/.intermediates/hardware/interfaces/common/aidl/android.hardware.common-V2-ndk-source/gen/include/aidl/android/hardware/common/
// $ cp -R *.h
// ~/chromiumos/src/platform2/nnapi/includes/aidl/android/hardware/common $ #
// Apply the changes as described above

#pragma once

#include <android/binder_interface_utils.h>
#include <android/binder_parcelable_utils.h>
#include <android/binder_to_string.h>
#include <cstdint>
#include <memory>
#include <optional>
#include <string>
#include <vector>
#ifdef BINDER_STABILITY_SUPPORT
#include <android/binder_stability.h>
#endif  // BINDER_STABILITY_SUPPORT

namespace aidl {
namespace android {
namespace hardware {
namespace common {
class NativeHandle {
 public:
  typedef std::false_type fixed_size;
  static const char* descriptor;

  std::vector<::ndk::ScopedFileDescriptor> fds;
  std::vector<int32_t> ints;

  binder_status_t readFromParcel(const AParcel*) { return 0; }
  binder_status_t writeToParcel(AParcel*) const { return 0; }

  inline bool operator!=(const NativeHandle& rhs) const {
    return std::tie(fds, ints) != std::tie(rhs.fds, rhs.ints);
  }
  inline bool operator<(const NativeHandle& rhs) const {
    return std::tie(fds, ints) < std::tie(rhs.fds, rhs.ints);
  }
  inline bool operator<=(const NativeHandle& rhs) const {
    return std::tie(fds, ints) <= std::tie(rhs.fds, rhs.ints);
  }
  inline bool operator==(const NativeHandle& rhs) const {
    return std::tie(fds, ints) == std::tie(rhs.fds, rhs.ints);
  }
  inline bool operator>(const NativeHandle& rhs) const {
    return std::tie(fds, ints) > std::tie(rhs.fds, rhs.ints);
  }
  inline bool operator>=(const NativeHandle& rhs) const {
    return std::tie(fds, ints) >= std::tie(rhs.fds, rhs.ints);
  }

  static const ::ndk::parcelable_stability_t _aidl_stability =
      ::ndk::STABILITY_VINTF;
  inline std::string toString() const {
    std::ostringstream os;
    os << "NativeHandle{";
    os << "fds: " << ::android::internal::ToString(fds);
    os << ", ints: " << ::android::internal::ToString(ints);
    os << "}";
    return os.str();
  }
};
}  // namespace common
}  // namespace hardware
}  // namespace android
}  // namespace aidl
