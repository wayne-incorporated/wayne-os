[package]
name = "kexec-lite"
version = "0.1.0"
edition = "2021"

[profile.release]
panic = "abort"
overflow-checks = true

[dependencies]
clap = { version = "3.2.23", features = ["cargo"] }
libc = "0.2.139"
