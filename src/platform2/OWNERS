# See project subdirectories for individual OWNERS files.

# We don't set general owners as we don't want subdirs to inherit these.
# per-file can apply to dirs too, so we need to limit to known files.
per-file ./LICENSE, /PRESUBMIT.cfg, /PRESUBMIT.py = file:/common-mk/OWNERS
